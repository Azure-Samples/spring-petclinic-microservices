spring:
  cloud:
    config:
      #uri: http://localhost:8888
      uri: http://172.19.70.211:8888
  application:
    name: customers-service
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8888
